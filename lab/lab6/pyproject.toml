[project]
name = "lab6-pthreads-heated-plate"
version = "0.1.0"
description = "Lab6 Pthreads parallel_for heated plate benchmark and plotting workflow"
requires-python = ">=3.10"
dependencies = [
  "matplotlib>=3.8",
  "pandas>=2.2",
]
